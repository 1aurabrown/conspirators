<!DOCTYPE html>
<html>
<head>
  <%= render 'head' %>
</head>

<body class="<%=params[:controller]%> <%= params[:action]%> <%=(@use_small_footer ? 'small-footer' : '')%>" data-controller="<%=params[:controller]%>">
  <%= include_gon(:init => true) %>
  <%= render partial: 'navigation' %>
  <%= content_for?(:content) ? yield(:content) : yield %>
  <%= render partial: 'footer' %>
  <%= render 'aside' %>
</body>
</html>
