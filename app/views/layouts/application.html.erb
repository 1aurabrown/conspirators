<!DOCTYPE html>
<html>
<head>
  <%= render 'head' %>
</head>

<body class="<%=params[:controller]%> <%= params[:action]%>" data-action="<%= params[:action] %>" data-controller="<%=params[:controller]%>">
  <%= include_gon(:init => true) %>
  <%= render partial: 'navigation' %>
  <%= content_for?(:content) ? yield(:content) : yield %>
  <%= render partial: 'footer' %>
  <%= render 'aside' %>
</body>
</html>
